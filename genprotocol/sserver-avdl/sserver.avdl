@namespace("sserver")
protocol searchServer {
  void writeIndex(bytes secureIndex, string docID);
  void renameIndex(string orig, string curr);
  array<string> searchWord(array<bytes> trapdoors); 
  array<bytes>  getSalts();
}
